---
title: "grad"
---

> **grad**(`f`): (`x`, `inputs?`) => `any`

Defined in: [api.js:189](https://github.com/tangent-to/grad/blob/2b49f114ab283e1b1f70b759a41ed9af1b885364/src/api.js#L189)

Gradient only, discarding the objective's value.

## Parameters

### f

(`x`, `inputs?`) => [`Var`](../classes/Var.md)

## Returns

gradient, shaped like the parameters

(`x`, `inputs?`) => `any`
